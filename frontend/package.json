{
  "name": "stripe-demo-frontend",
  "version": "1.0.0",
  "description": "Frontend for Stripe card on file demo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && echo '/* /index.html 200' > dist/_redirects",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@stripe/stripe-js": "^2.2.0"
  }
}
